# Ignore logs and checkpoints
.ipynb_checkpoints/
*.pyc
*.log

# Ignore generated data while allowing curated runtime inputs
data/*
!data/curated/
!data/curated/**
*.zip
*.pickle

# Preserve the original prototype interchange snapshot
!notebooks/legacy/interchanges.pickle

# Keep the small interchange inspection CSV tracked for validation
!_interchanges_csv_for_inspection_only.csv

# Ignore environment files, API keys, secrets
.env
config/secrets.json
keys/
